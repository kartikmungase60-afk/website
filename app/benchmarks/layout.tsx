import { createPageMetadata } from "../seo"
export const metadata = createPageMetadata({ title: "Hosting Hardware Benchmark Methodology", description: "Learn how to evaluate CPU, NVMe storage, network routes and application performance when comparing hosting plans.", path: "/benchmarks", keywords: ["hosting benchmarks", "VPS performance testing", "Minecraft server benchmark", "NVMe hosting performance"] })
export default function Layout({ children }: { children: React.ReactNode }) { return children }
