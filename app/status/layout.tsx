import { createPageMetadata } from "../seo"
export const metadata = createPageMetadata({ title: "Service Status & Infrastructure Monitoring", description: "Review Hostlixo service monitoring coverage, maintenance communication and incident support information across global hosting regions.", path: "/status", keywords: ["Hostlixo status", "hosting service status", "server monitoring", "cloud infrastructure status"] })
export default function Layout({ children }: { children: React.ReactNode }) { return children }
